import("/nav.js");
const $=(id)=>document.getElementById(id);const safe=(value)=>String(value??"").replace(/[&<>'"]/g,(char)=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"})[char]);let csrfToken="";
const api=async(url,options={})=>fetch(url,{...options,headers:{...(options.body?{"content-type":"application/json"}:{}),...(options.method&&options.method!=="GET"?{"x-csrf-token":csrfToken}:{}),...options.headers}});
async function initialize(){const response=await fetch("/api/v1/auth/session");if(!response.ok){location.href="/";return;}csrfToken=(await response.json()).csrf_token;await refresh();}
async function refresh(){const direction=$("filter-direction").value,channel=$("filter-channel").value,query=new URLSearchParams();if(direction)query.set("direction",direction);if(channel)query.set("channel",channel);const[status,airtime,queue,messages]=await Promise.all([api("/api/v1/status").then(r=>r.json()),api("/api/v1/mesh/airtime").then(r=>r.json()),api("/api/v1/mesh/queue").then(r=>r.json()),api(`/api/v1/mesh/messages?limit=100&${query}`).then(r=>r.json())]);const state=$("link-state");state.className=`status ${status.radio}`;state.innerHTML=`<i></i>${safe(status.radio)}`;$("radio-node").textContent=status.radio_config.node_id||"—";$("radio-preset").textContent=`${status.radio_config.region} · ${status.radio_config.preset}`;$("airtime-total").textContent=airtime.used_seconds.toFixed(2);$("queue-count").textContent=queue.items.length;$("message-count").textContent=messages.items.length;const maximum=Math.max(1,...Object.values(airtime.by_class_seconds));$("airtime-bars").innerHTML=Object.entries(airtime.by_class_seconds).map(([name,value])=>`<div class="airtime-row"><span>${safe(name)}</span><div class="airtime-track"><i style="width:${safe(value/maximum*100)}%"></i></div><strong>${Number(value).toFixed(2)}s</strong></div>`).join("");$("queue-list").innerHTML=queue.items.map(item=>`<article class="queue-card"><strong>#${item.id} · ${safe(item.traffic_class)} → ${safe(item.destination)}</strong><p>${safe(item.text)}</p><button data-cancel="${item.id}">Cancel item</button></article>`).join("")||`<p class="empty">Queue empty.</p>`;document.querySelectorAll("[data-cancel]").forEach(button=>button.addEventListener("click",async()=>{await api(`/api/v1/mesh/queue/${button.dataset.cancel}`,{method:"DELETE"});await refresh();}));$("message-rows").innerHTML=messages.items.map(message=>`<tr><td>${new Date(message.created_at).toLocaleTimeString()}</td><td>${message.direction==="in"?"↙ RX":"↗ TX"}</td><td><code>${safe(message.peer_mesh_id||"broadcast")}</code></td><td>${message.channel}</td><td>${safe(message.text||`${message.byte_len} bytes`)}</td><td>${safe(message.outcome||"—")}</td><td>${message.rx_snr==null?"—":`${safe(message.rx_snr)} dB`}</td></tr>`).join("")||`<tr><td colspan="7">No matching messages.</td></tr>`;}
$("send-form").addEventListener("submit",async(event)=>{event.preventDefault();$("send-result").textContent="";const response=await api("/api/v1/mesh/send",{method:"POST",body:JSON.stringify({text:$("send-text").value,destination:$("send-destination").value,channel:Number($("send-channel").value),traffic_class:$("send-class").value})});const body=await response.json();if(!response.ok){$("send-result").textContent=body.error.message;return;}$("send-result").textContent=`Queued as item #${body.queue_id}`;$("send-text").value="";await refresh();});
$("refresh-radio").addEventListener("click",refresh);$("filter-direction").addEventListener("change",refresh);$("filter-channel").addEventListener("change",refresh);initialize();setInterval(()=>{if(csrfToken)refresh();},10000);
